import { Layout, SEO } from "components/layout";
import React from "react";

export const AuthorList = (data): JSX.Element => {
  return (
    <>
      <SEO />
      <Layout>Wow</Layout>
    </>
  );
};
