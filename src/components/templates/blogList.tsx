import { Layout, SEO } from "components/layout";
import React from "react";

export const BlogList = (data): JSX.Element => {
  return (
    <>
      <SEO />
      <Layout>Wow</Layout>
    </>
  );
};
